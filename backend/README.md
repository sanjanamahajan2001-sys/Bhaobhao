1. Create a `.env` file in the project root and add the following:

    - ```env with server values
    DATABASE_URL=postgresql://postgres:password@localhost:5432/pawfectcare
    APP_PORT=5000
    SERVER_BASE_URL=http://localhost:5000


2. Use npm to install the project dependencies:

    - npm install


3. Run app in dev environment

    - npm run dev